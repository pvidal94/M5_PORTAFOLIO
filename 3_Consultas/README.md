## 3. Consultas (SQL SELECT)

Esta carpeta contiene los scripts de consulta SQL (SELECT) diseñados para obtener la información

📜 Archivo

3_Consultas.sql

## 🎯 Propósito

El propósito de este script es demostrar cómo obtener información valiosa y agregada desde la base de datos. Estas consultas simulan las llamadas que haría el backend (como Django) para construir los indicadores del dashboard.

Ejemplos de consultas incluidas:

Conteo de proyectos activos (COUNT).

Cálculo de presupuesto total vs. gastado (SUM y GROUP BY).

Identificación de tareas atrasadas (JOIN y WHERE).

## 🏃‍♂️ Ejecución

Este script se ejecuta después de 02_data_dml.sql. Se puede ejecutar en cualquier momento para leer el estado actual de la base de datos sin modificar ningún dato.